# Production — Postgres + Redis + nginx TLS, full security
# Usage: docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
#
# Required .env.prod:
#   DB_PASSWORD=strong-random-password
#   REDIS_PASSWORD=another-strong-password
#   ADMIN_API_KEY=your-admin-key-min-32-chars
#   ENCRYPTION_KEY=hex-encoded-32-byte-key
#   ISSUER=https://auth.myapp.com
#   CORS_ORIGINS=https://myapp.com,https://admin.myapp.com
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_USERNAME=apikey
#   SMTP_PASSWORD=SG.xxxxx
#   SMTP_FROM=noreply@myapp.com
#   SMS_ACCOUNT_ID=ACxxxxx (optional)
#   SMS_AUTH_TOKEN=xxxxx (optional)
#   SMS_FROM_NUMBER=+1234567890 (optional)

services:
  authcore:
    image: authcore:latest
    build: .
    ports:
      - "8080:8080"
    environment:
      AUTHCORE_ENV: production
      AUTHCORE_HTTP_PORT: 8080
      AUTHCORE_DATABASE_DSN: postgres://authcore:${DB_PASSWORD}@postgres:5432/authcore?sslmode=require
      AUTHCORE_REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      AUTHCORE_ADMIN_API_KEY: ${ADMIN_API_KEY}
      AUTHCORE_ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      AUTHCORE_ISSUER: ${ISSUER}
      AUTHCORE_CORS_ORIGINS: ${CORS_ORIGINS}
      AUTHCORE_TENANT_MODE: header
      AUTHCORE_KEY_ROTATION_DAYS: "90"
      AUTHCORE_SMTP_HOST: ${SMTP_HOST:-}
      AUTHCORE_SMTP_PORT: "587"
      AUTHCORE_SMTP_USERNAME: ${SMTP_USERNAME:-}
      AUTHCORE_SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      AUTHCORE_SMTP_FROM: ${SMTP_FROM:-noreply@authcore.local}
      AUTHCORE_SMS_PROVIDER: ${SMS_PROVIDER:-}
      AUTHCORE_SMS_ACCOUNT_ID: ${SMS_ACCOUNT_ID:-}
      AUTHCORE_SMS_AUTH_TOKEN: ${SMS_AUTH_TOKEN:-}
      AUTHCORE_SMS_FROM_NUMBER: ${SMS_FROM_NUMBER:-}
      AUTHCORE_WEBAUTHN_RP_ID: ${WEBAUTHN_RP_ID:-localhost}
      AUTHCORE_WEBAUTHN_RP_NAME: ${WEBAUTHN_RP_NAME:-AuthCore}
      AUTHCORE_WEBAUTHN_RP_ORIGINS: ${WEBAUTHN_RP_ORIGINS:-http://localhost:8080}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    deploy:
      replicas: 2
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: authcore
      POSTGRES_USER: authcore
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata_prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U authcore"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
    deploy:
      resources:
        limits:
          memory: 128M

  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      authcore:
        condition: service_healthy
    restart: always

volumes:
  pgdata_prod:
